# Example maintainer quickstart

This placeholder-only example is a review aid. No release happened; no build, tag, publish,
upload, or deployment happened. Maintainer review and private review remain required.

1. Run `COMMAND_PLACEHOLDER` for local setup and safety checks.
2. Run `make try-demo` for synthetic local Demo Mode only.
3. Read `HANDOFF_DOMAIN_PLACEHOLDER` and record `PRIVATE_REVIEW_REF_PLACEHOLDER` privately.
4. Stop until `REVIEW_DECISION_PLACEHOLDER` is recorded outside Git.

Do not add real credentials, customer IDs, domains, URLs, evidence, reports, logs, generated
outputs, or approval records to this example.
