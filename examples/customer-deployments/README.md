# Fake customer deployment examples

Every value in this directory is fake or a placeholder. These examples contain no real
customer names, IDs, projects, domains, contacts, credentials, storage resources, URLs, or
private paths. They are local planning inputs only and do not deploy anything.

Generated artifacts belong under the ignored `customer-output/` directory and must not be
committed.
