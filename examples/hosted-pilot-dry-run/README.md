# Hosted pilot dry-run example

This public example contains opaque placeholders only. It performs no live operation, reads no
private report or evidence content, and does not represent launch or pilot approval.

Use `make hosted-pilot-dry-run-check` for offline validation. Keep real hosted and pilot work in
an ignored private workspace under human control.
