# API Documentation Review Examples

These Phase J3 examples are placeholder-only illustrations. They contain no real route values,
identities, domains, URLs, IDs, paths, credentials, tokens, reports, payloads, logs, private data,
or approval claims. They do not make API calls or use external OpenAPI tooling.

Use the canonical guides in `docs/` and generate any local report only into an ignored output root.
