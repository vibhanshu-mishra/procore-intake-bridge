# PostgreSQL runtime examples

These files contain placeholders only. Copy them into an approved private workspace before adding
references. They contain no database URL, host, database name, username, backup name, dump name,
log, or private path. The examples do not connect, migrate, back up, or restore.
