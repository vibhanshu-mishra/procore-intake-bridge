# Sandbox read-validation examples

These files are fake placeholder templates only. They make no Procore calls and contain no real
scope, identifiers, people, URLs, paths, payloads, credentials, or report contents.

- `example_read_validation_plan.json` describes bounded read-only posture.
- `example_read_evidence_ref.json` shows the private reference shape for later review.
