server {
    listen 8080;
    server_name ALLOWED_HOST_PLACEHOLDER;
    # TLS_CERT_REF_PLACEHOLDER and WEBHOOK_INGRESS_REF_PLACEHOLDER only.
    location /webhooks/procore {
        proxy_pass http://APP_UPSTREAM_PLACEHOLDER;
    }
}
