# Deployment recipe examples

These files are placeholder-only D4 planning inputs. They do not provision, deploy, call a cloud
API, configure DNS, issue certificates, register webhooks, or contain production configuration.
