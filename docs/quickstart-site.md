# Quickstart

The canonical setup path remains the repository-level
[Quickstart](../QUICKSTART.md). Keeping one source avoids conflicting install or command order.

Demo Mode does not require MkDocs or this navigation layer. Start with `make start`, then
`make try-demo`. Use the [Demo walkthrough](walkthrough-demo.md) for the guided journey.

This page is only a docs-site navigation gateway. It performs no install, build, publication,
deployment, or external call.
